import styled from "styled-components"

export const Container = styled.div`
    input, button {
        border: none;
    }

    input {
        height: 3.6rem;
    }

    button {
        border-radius: 0 !important;
    }
`